# 作業ログ 2026-05-19

## 作業内容

### JSONスキーマ仕様変更（ArticulationGroups 導入）に伴うUIの改善

#### 方針決定
- UIレイアウト: グループドロップダウン切替方式（案B）を採用
- FormatVersion: 0 のまま維持
- 旧形式（Articulations フラット配列）の互換性: なし（エラー）

#### 変更ファイル

**`apps/app.js`**
- `state.selectedIndex` → `state.selectedGroupIndex` + `state.selectedArticIndex` に分離
- `newEmptyDefinition()`: `Articulations[]` → `ArticulationGroups[{Name, Articulations[], Extra}]` に変更
- `newArticulationGroup()` ヘルパー追加
- `renderGroupSelect()` 新規追加（グループドロップダウン・グループ名入力の描画）
- `renderArticulationList()`: 選択グループ配下のアーティキュレーション表示に変更
- `renderArticulationEdit()`: グループ→アーティキュレーションのアクセスパスに変更
- `getExtraObject()`: `'group'` ターゲット追加（ArticulationGroup の Extra 編集対応）
- `exportYaml()`: `ArticulationGroups` ネスト構造に変更
- `loadDefinitionFromObj()`: `ArticulationGroups` 読み込み対応、旧形式エラー処理追加
- グループ操作イベントハンドラ追加（追加・削除・選択切替・リネーム）
- リサイズハンドル（`#resize-handle`）追加：Articulations リストと Edit パネルの境界をマウスドラッグで変更可能に

**`apps/index.html`**
- グループコントロール UI 追加（`#group-controls`）
  - グループセレクタ（`#group-select`）
  - グループ追加/削除ボタン（`#btn-add-group` / `#btn-del-group`）
  - グループ名入力（`#group-name-input`）
  - グループ Extra 編集ボタン（`#btn-group-extra`）
- `#btn-add-articulation` をフッターエリア（`#artic-list-footer`）に移動
- リサイズハンドル要素（`#resize-handle`）追加

**`apps/style.css`**
- `#group-controls`・`.group-ctrl-row`・`#group-select`・`#group-name-input`・`#btn-add-group`・`#btn-del-group` のスタイル追加
- `#artic-list-footer`・`#btn-add-articulation` のスタイル追加
- `#resize-handle` のスタイル追加

## 動作確認

- 動作確認完了
- リグレッションテスト完了

---

### 新規 Articulation 作成時の MidiMessages 引き継ぎ機能追加

#### 変更ファイル

**`apps/index.html`**
- `#artic-list-footer` にチェックボックス（`#chk-inherit-midi`）とラベルを追加
  - デフォルト: チェック済み（引き継ぐ）

**`apps/style.css`**
- `#inherit-midi-label` のスタイル追加

**`apps/app.js`**
- `#btn-add-articulation` クリック時に `#chk-inherit-midi` の状態を参照
  - チェックオン かつ 直前のアーティキュレーションが存在する場合：MidiMessages をシャローコピーして引き継ぐ
  - チェックオフ または グループ内が空の場合：空の MidiMessages で作成

## 動作確認（2回目）

- 動作確認完了
- リグレッションテスト完了
