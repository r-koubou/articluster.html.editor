# 2026-05-05 作業ログ

## 作業内容

### エディタ新規実装（`apps/index.html` → 後にファイル分割）

#### 仕様確認
- `docs/specs/README.md`、`docs/specs/ui/README.md`、`docs/specs/universal-definition-schema.json`、UI ワイヤーフレーム画像（appendix/*.jpg）の内容を確認した。
- 不明点として Description フィールドの要否・YAML パーサーの実装方針を人間に確認し、以下の方針を決定した。
  - Description: General Edit に含める
  - YAML パーサー: 外部ライブラリ不使用のカスタム実装

#### 実装
以下のファイルを新規作成した。

| ファイル | 概要 |
|---|---|
| `apps/index.html` | HTML（CSS / JS を外部参照） |
| `apps/style.css` | スタイルシート |
| `apps/app.js` | アプリケーションロジック |

**実装機能**
- Header: New / Load / Save ボタン
- General Edit: ID / Author / ManufacturerName / ProductName / PatchName / Description / Extra 編集
- Articulation List: 追加（`[+]`）・削除（`[✕]`）・選択
- Articulation Edit: Name・MidiMessages（Status ドロップダウン / Channel / Data1 / Data2）・Extra 編集
- Extra Editor: モーダルダイアログで Key-Value ペアの追加・削除・編集
- YAML エクスポート: スキーマ準拠、2スペースインデント、Status = statusType | channel
- YAML インポート: YamlDotNet 出力形式対応カスタムパーサー
- トースト通知: 成功（緑 2秒）・エラー（赤 3秒）

#### バグ修正（YAML インポート）
人間による動作確認で Articulation が読み込まれないバグを報告された。

**原因**: YamlDotNet はシーケンスを親マッピングキーと同じインデントで出力するが（`Articulations:\n- Name: ...`）、パーサーが「子は必ず親より深いインデント」と判定していたため読み込みをスキップしていた。また UTF-8 BOM が付与されたファイルでパースが失敗する問題もあった。

**修正内容**: 同インデントの `- ` 行も子シーケンスとして受け入れるよう条件を変更。パース前に BOM（`﻿`）を除去する処理を追加。

#### 動作確認結果
- New / General Edit / Articulation List / Articulation Edit / Extra Modal: 正常動作
- YAML エクスポート: スキーマ準拠の内容で出力されることを確認
- YAML インポート: バグ修正後、Articulation を含むファイルが正しく読み込まれることを確認

---

### エディタ UI デザイン改善

#### 仕様確認・方向性確認
人間へ以下3点を確認し回答を得た。
- プライマリカラー: Indigo (`#3949AB`)
- ヘッダボタン: 白背景 + Indigo テキスト（アウトラインスタイル）
- タイトル領域: 薄いプライマリ系背景 + プライマリ系テキスト

#### 実装
`apps/style.css` をマテリアルデザイン配色に更新した。

| 対象 | 変更内容 |
|---|---|
| ヘッダ背景 | 白 → Indigo 600 `#3949AB` |
| ヘッダ文字 | 黒 → 白 |
| ヘッダボタン | 白 + グレーボーダー → 白背景 + Indigo テキスト + 白透過ボーダー |
| 一般ボタン | 白 + グレーボーダー → Indigo 600 + 白文字 |
| セクションタイトル (h2) | グレー文字のみ → Indigo 50 全幅帯 + Indigo 800 文字 |
| Articulations リストヘッダ | 白背景 → Indigo 50 背景 + Indigo 800 文字 |
| 選択中リストアイテム | `#e8f0fb` → Indigo 100 `#C5CAE9` |
| モーダルヘッダ | 白のみ → Indigo 50 背景 + Indigo 800 文字 |
| フォーカス枠色 | `#4a90d9` → Indigo 600 `#3949AB` |

#### 動作確認結果
- UI 全体でマテリアルデザイン Indigo カラーが適用されていることを確認
- ヘッダ・ボタン・タイトル領域・モーダルすべて正常表示を確認
- 機能のリグレッションなし

---

### フッター追加

#### 方向性確認
- 配置: 右寄せテキスト
- カラー: ヘッダーと同じ Indigo 600 (`#3949AB`) で統一

#### 実装

| ファイル | 変更内容 |
|---|---|
| `apps/index.html` | `<footer id="footer">` を追加（GitHubリンク + Copyright）|
| `apps/style.css` | `#footer` スタイル追加（Indigo背景・右寄せ・リンク装飾） |

**追加要素**
- GitHub リポジトリリンク（`target="_blank" rel="noopener"` 付き）
- Copyright: `(c) R-Koubou All rights reserved.`

#### 動作確認結果
- フッターが画面下部に常時表示されることを確認
- GitHubリンクが新しいタブで正しく開くことを確認
- 右寄せレイアウト・Indigoカラー適用を確認
- 機能のリグレッションなし
