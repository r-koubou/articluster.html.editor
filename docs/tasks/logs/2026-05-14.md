# 作業ログ 2026-05-14

## 作業内容

### ファイルフォーマットバージョン値の追加（`apps/app.js`）

- `FORMAT_VERSION = 1` 定数を追加
- `newEmptyDefinition()` に `FormatVersion: FORMAT_VERSION` フィールドを追加
- `loadDefinitionFromObj()` 冒頭に `FormatVersion` バリデーションを追加
  - フィールドが存在しない場合：アラートダイアログを表示してロード中止（`null` 返却）
  - 値が `FORMAT_VERSION` と一致しない場合：アラートダイアログを表示してロード中止（`null` 返却）
- ファイルロードハンドラで `loadDefinitionFromObj` が `null` を返したとき早期リターンするよう修正
- `exportYaml()` の先頭行に `FormatVersion: 1` を出力するよう修正

## 動作確認結果

問題なし（ユーザー確認済み）

---

## 作業内容

### MIDIノートオン/オフの Data1 をドロップダウンに変更（`apps/app.js`）

- `NOTE_NAMES`、`MIDI_NOTE_LABELS` 定数を追加（YAMAHA方式: MIDI 60 = C3）
- `buildD1NumberInput(msg)` ヘルパー関数を追加（従来の数値入力フィールド）
- `buildD1NoteSelect(msg)` ヘルパー関数を追加（音階名ドロップダウン）
  - 選択肢の形式: `<ノートナンバー>: <音階名>` （例: `60: C3`）
  - option の value にはノートナンバー（数値）を設定し、YAML出力値と一致させる
- `createMidiRow()` を修正
  - Data1 を `d1Wrap` コンテナで包み、Status に応じてドロップダウン/数値入力を動的切り替え
  - Note On / Note Off 選択時 → ドロップダウン
  - その他のタイプ選択時 → 数値入力フィールド
- YAML import / export は変更なし（Data1 は引き続き 0〜127 の数値で保持・出力）

## 動作確認結果

問題なし（ユーザー確認済み）
